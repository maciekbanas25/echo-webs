# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
FULL_SETUP.sql

# Ruflo / claude-flow runtime data (machine-local, not for deploy)
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/
.claude-flow/metrics/
.claude-flow/learning/
.claude/settings.local.json

# Design reference files (Refero DESIGN.md, etc) — reference-only, not deployed
design-refs/

# Obsidian vault symlinks (knowledge base, machine-local)
# No trailing slash: these are symlinks (git treats them as files, not dirs)
notes
notes-shared
